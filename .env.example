# Required
DOMAIN=example.com
SESSION_SECRET=replace-with-long-random-string
ADMIN_BOOTSTRAP_PASSWORD=replace-me
ADMIN_BOOTSTRAP_USER=admin

# Public API hostname (clients use https://HOST/v1).
# Local compose: leave empty → http://localhost:9081/v1
# Traefik (compose.traefik.yaml): required for the Host rule.
PUBLIC_HOST=llm-gateway.example.com

# Only for local compose.yaml (published host ports). Not used behind Traefik /
# a reverse proxy — there you hit https://PUBLIC_HOST, no :9080/:9081.
# WEB_PORT=9080
# GATEWAY_PORT=9081

# Optional: seed named sources into SQLite on first start only (empty DB).
# After that, manage sources in Admin → Services. Env vars are ignored once any source exists.
# CHAT_SOURCE=192.168.1.10:11535
# CHAT2_SOURCE=192.168.1.10:11435
# EMBED_SOURCE=192.168.1.10:11536
# STT_SOURCE=192.168.1.10:9000
# TTS_SOURCE=192.168.1.10:9001

# compose.traefik.yaml only
TEMP_MAX_C=30
TEMP_SENSOR_TYPE=x86_pkg_temp

# Optional: show Dashboard “Seed demo data” (usage charts polish). Default off.
# DEMO_TOOLS=1

# Optional override. Gateway + integration reports already use
# http://<source-host>:9105/power from CHAT_SOURCE / the call host.
# Deploy on the GPU/source host: docker compose -f services/gpu-power/compose.yaml up -d --build
# GPU_POWER_URL=
# GPU_POWER_PORT=9105
